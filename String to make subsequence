class Solution {
    public int appendCharacters(String s, String t) {
        int match=0;
        for(int i=0;i<s.length();i++){
            if(match < t.length() && s.charAt(i)==t.charAt(match)){
                match++;
            }
        }
        return t.length()-match;
    }
}
